## 第一章：实现一个简单的Bean容器


## 第二章：实现Bean的定义、注册、获取
编码方式主要依托于：接口定义、类实现接口、抽象类实现接口、继承类、继承抽象类，这些操作方式可以很好的隔离开每个类的基础功能、通用功能和业务功能，当类的职责清晰后，整个设计也会变得容易扩展和迭代
***
目标：把bean的创建交给容器，而不是在调用的时候传递一个实例化的Bean对象，另外还需考虑单例对象，在二次获取对象时直接从内存中获取。
***
问题：  
1. 对象是直接调用Class.newInstance()实现的，如果该类没有无参构造器则会创建失败。
2. 对象中如果有成员变量要赋值，未提供注入值的方式。
***
总结：  
* 在Spring容器的实现类中要重点关注类之间的职责和关系，几乎所有的程序功能设计都离不开接口、抽象类、实现、继承，而
这些不同特性类的使用可以非常好的隔离开类的功能职责和作用范围。
* 一个有成长的程序员要记住代码实现只是最后的落地结果，而那些设计上的思考才是最有价值的地方。不要只是被看似简单的内容忽略了
整体全局观，要学会放开事业，开放学习视角。


## 第三章：基于CGLib实现带参构造器的类实例化策略
当你在接一个产品需求时，开始思考`程序数据结构的设计`、`核心功能的算法逻辑实现`、`整体服务的设计模式使用`、`系统架构的搭建方式`、`应用集群的部署结构`，
那么就是变成能力真正提升的时候。
***
目标：按照是否包含构造函数实现不同的实例化策略
1. CGLib实现
2. JDK中的反射Constructor
***
问题：  
1. 在AbstractAutowireCapableBeanFactory.createBeanInstance方法中，对于Constructor的判断是通过参数个数判断的，
这种判断不严谨，应该通过类型+个数判断。
2. 构造方法的参数类型不一致，在创建对象时会直接报错，可以添加类型转换器转换为对应类型再创建对象，无法转换抛出相应异常。
3. CGLIB3.3.0版本无法兼容JDK23，所以需要Spring6中的CGLIB库
***
总结：  
* 从不断地完善增加需求可以看出，当代码结构设计的较为合理时，可以非常容易且方便地进行扩展不同属性的类的职责，而不会因为需求的
增加导致类结构混乱。所以在业务需求实现的过程中，要尽可能地考虑一个良好的扩展性以及拆分好类的职责。
* 将想法融入到代码中，多敲代码。


## 第五章：注入属性和依赖对象
目标：给对象中的基本类型、引用类型的成员变量赋值
***
总结：  
* 本章把AbstractAutowireCapableBeanFactory类中的创建对象功能做了扩充，在对象创建完成之后，开始对对象的成员变量
赋值。当变量为引用类型时，需要递归处理。最后通过反射给对象变量赋值。
* 学习编程有的时候学习思路设计要比仅仅做简单实现，更能提升编程思维。


## 第六章：资源加载器解析文件注册对象
目标：将Bean定义、注册、属性填充这部分操作放到配置文件中进行处理，通过读取配置文件在框架中自动实现这部分流程。

***
需要在现有的Spring框架雏形中添加一个资源解析器，即能读取classpath、本地文件和云文件的配置内容。这些配置内容像使用Spring时配置的Spring.xml一样，
里面会包括Bean对象的描述和属性信息。在读取配置文件信息后，接下来是对配置文件中的Bean描述信息解析后进行注册，把Bean对象注册到Spring容器中。
* 资源加载器属于相对独立的部分，它位于Spring框架核心包下的IO实现内容，主要用于处理Class、本地和云环境中的文件信息。
* 当资源加载后，接下来就是解析和注册Bean到Spring中的操作，这部分实现需要和`DefaultListableBeanFactory`核心类结合起来，因为所有解析后的注册动作，
都把Bean定义信息放到了这个类中。
* 在实现的时候设计好接口的实现层级关系，包括需要定义出Bean定义的读取接口`BeanDefinitionReader`以及做好对应的实现类，在实现类中完成Bean对象的解析和注册。

## 第七章：实现应用上下文，自动识别、资源加载、扩展机制
在对容器中Bean的实例化过程添加扩展机制的同时，需要把目前关于Spring.xml初始化和加载策略进行优化，因为我们不太可能让面向Spring本身开发的
DefaultListableBeanFactory服务，直接给予用于使用。修改点如下：
* DefaultListableBeanFactory、XmlBeanDefinitionReader，是目前Spring框架中对于服务功能测试的使用方式，他能体现出Spring是如何对xml加载
以及注册Bean对象的操作过程，但这种方式是面向Spring本身，还不具备一定的扩展性。
* 需要提供一个可以在Bean初始化过程中，完成对Bean对象的扩展时，很难做到自动化处理。所以要把Bean对象扩展机制功能和对Spring框架上下文的包装融合起来，
对外提供完整的服务。

***
为了满足于在Bean对象从注册到实例化的过程中执行用于的自定义操作，需要在Bean的定义和初始化过程中插入接口类，这个接口再由外部去实现自己需要的服务。
* 满足于对Bean对象扩展的两个接口，即Spring框架中非常具有重量级的接口：BeanFactoryPostProcessor、BeanPostProcessor。
* BeanFactoryPostProcessor，是由Spring框架组件提供的容器扩展机制，允许在Bean对象注册后但未实例化之前，**对Bean的定义信息BeanDefinition执行修改操作**。
* BeanPostProcessor，在Bean对象实例化之后修改Bean对象，也可以替换Bean对象。**这部分与后面要实现的AOP有密切的关系**。
* 同时如果只是添加两个接口，不做任何包装，那么对于使用者来说非常麻烦。我们希望编写一个Spring的上下文操作类，把相应的XML加载、注册、实例化以及新增的修改和
扩展都融合进去，让Spring可以自动扫描到我们的新增服务，便于用户使用。

***
执行流程  
1. 创建BeanFactory对象
2. 创建XmlBeanDefinitionReader对象
3. 解析xml配置文件，将BeanDefinition对象注册到BeanFactory中
4. 获取所有的BeanFactoryPostProcessor对象，并执行postProcessBeanFactory()方法
5. 将BeanPostProcessor对象注册到BeanFactory中
6. 实例化所有单例Bean对象
   1. 通过反射或者CGLib创建Bean对象
   2. 根据PropertyValues中的数据对Bean对象的变量赋值
   3. 调用BeanPostProcessor的postProcessBeforeInitialization()方法
   4. 调用BeanFactory的invokeInitMethods()方法（bean的初始化方法，第八章）
   5. 调用BeanPostProcessor的postProcessAfterInitialization()方法
   6. 将单例Bean对象保存到映射表中


## 第八章：向虚拟机注册钩子，实现Bean对象的初始化和销毁方法
从易阅读来说，代码要有准确的命名和清晰的注释；从易使用来说，代码要具备设计模式的包装让对外的服务调用更简单；从易扩展来说，代码要做好业务和功能
的实现分层。在易阅读、易使用、易扩展以及更多编码规范的约束下，需要在开发完成上线后的交付结果满足：高可用、高性能、高并发。
***

目标：满足用户可以在xml中配置初始化和销毁方法，也可以通过实现类的方式处理，比如在使用Spring时用到的InitializingBean、DisposableBean接口。
***

总结：  
在学习和动手实践Spring框架学习的过程中，特别要注意的时它对接口和抽象类的把握和使用，尤其遇到类似，A继承B实现C时，C的接口方法由A继承的父类B实现。这种
方式可以复用到通常的业务系统开发中进行处理一些复杂逻辑的功能分层，做到程序的可扩展、易维护等特性。

## 第九章：定义标记类型Aware接口，实现感知容器对象
目标：  
目前已实现的Spring框架，在Bean操作上能提供出的能力，包括：Bean对象的定义和注册，以及在操作Bean对象过程中执行的，BeanFacotryPostProcessor、
BeanPostProcessor、InitializingBean、DisposableBean，以及在XML新增的一些配置处理，可以让Bean对象有更强的操作性。  
那么，如果想获得Spring框架提供的BeanFactory、ApplicationContext、BeanClassLoader等这些能力做一些扩展框架的使用时该怎么操作呢。所以，本章
希望在Spring框架中提供一种能感知容器操作的接口，如果谁实现了这样的接口，就可以获取接口入参中的各类能力。
***

设计：  
* 定义接口Aware，在Spring框架中它是一种感知标记性接口，具体的子类定义和实现能感知容器中的相关对象。也就是通过这个桥梁，向具体的实现类中提供容器服务
* 继承Aware的接口包括：BeanFactoryAware、BeanClassLoaderAware、BeanNameAware和ApplicationContextAware，当然在Spring源码中还有一些
其他关于注解的。
* 在具体的接口实现过程中可以看到：一部分（BeanFactoryAware、BeanClassLoaderAware、BeanNameAware）在factory目录下，另外
ApplicationContextAware是在context下，这是因为不同的内容获取需要在不同的包下提供。所以，在AbstractApplicationContext的具体实现中
会用到向beanFactory添加BeanPostProcessor的内容的ApplicationContextAwareProcessor操作，最后由AbstractAutowireCapableBeanFactory
创建createBean时处理相应的调用操作。
***

总结：  
每一章节内容的实现都是在以设计模式为核心的结构上填充各项模块的功能，单纯的操作编写代码并不会有太多收获，一定是要理解为什么这么设计，这么设计的好处是什么，
怎么有那么多的接口和抽象类的应用，这些才是Spring框架学习的核心所在。


## 第十章：对象作用域和FactoryBean
目标：  
交给Spring管理的Bean对象，一定就是我们用类创建出来的Bean吗？创建出来的Bean就永远是单例的吗，没有可能时原型模式吗？  
  
在集合Spring框架下，我们使用的MyBatis框架中，它的核心作用是可以满足用户不需要实现DAO接口类，就可以通过xml或注解配置的方式完成对数据库执行CRUD操作，
那么在实现这样的ORM框架中，是怎么把一个数据库操作的Bean对象交给Spring管理的呢。  
  
因为我们在使用Spring、MyBatis框架的时候可以知道，并没有手动地创建任何操作数据库的Bean对象，有的仅仅是一个接口定义，而这个接口定义竟然可以被注入到其他
需要使用DAO的属性中去，那么这一过程最核心待解决的问题，就是需要完成把复杂且以代理方式动态变化的对象，注册到Spring容器中。而为了满足这样的一个扩展组件
开发的需求，就需要在现有手写的Spring框架中，添加这一能力。
***

方案：  
* 整个实现过程包括两部分：一个解决单例还是原型对象，另一个处理FactoryBean类型对象创建过程中关于获取具体调用对象的getObject操作。
* SCOPE_SINGLETON、SCOPE_PROTOTYPE，对象类型的创建获取方式主要区分在于AbstractAutowireCapableBeanFactory#createBean创建完成对象后
是否放入到内存中，如果不放入则每次获取都会创建对象。
* createBean执行对象创建、属性填充、依赖加载、前置后置处理、初始化等操作后，就要开始做执行判断整个对象是否是一个FactoryBean对象，如果是这样的对象，
就需要再继续执行获取FactoryBean具体对象中的getObject对象了，整个getBean过程中都会新增一个单例类型的判断factory.isSingleton()，用于决定是否使用
内存存放对象信息。




## 实现项目
1. 数据库可视化管理工具，类似于DataGrip
2. 项目部署流水线，可视化工具，编译、部署、安全扫描、SSDLC一致性检查

## 项目开发流程
1. 需求分析：说明需求背景或来源以及业务系统的现状和问题，需要通过此需求实现哪些目标，能带来哪些价值。介绍需求中所涉及的额系统/产品，需要修改或新增的功能，画出变化点和流程图
2. 需求评审：对新增的需求进行验证是否通过
3. 开发设计评审：设计人员陈述需求内容、讲解设计方案和文档，方案讨论和确认，针对架构设计、编码设计、安全设计、接口设计、功能复用等关键要素进行评估
4. 测试验证
5. 开发代码评审

bugstack.cn